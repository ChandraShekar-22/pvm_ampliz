import { TexthighlightPipe } from './texthighlight.pipe';

describe('TexthighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new TexthighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
