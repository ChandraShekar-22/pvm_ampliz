import { MaskmobilePipe } from './maskmobile.pipe';

describe('MaskmobilePipe', () => {
  it('create an instance', () => {
    const pipe = new MaskmobilePipe();
    expect(pipe).toBeTruthy();
  });
});
