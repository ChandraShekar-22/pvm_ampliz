import { SearchPayorExecutive } from './search-payor-executive.model';

describe('SearchPayorExecutive', () => {
  it('should create an instance', () => {
    expect(new SearchPayorExecutive()).toBeTruthy();
  });
});
